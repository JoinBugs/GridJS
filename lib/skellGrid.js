( function( window, document, undefined )
{
	var util = {},
	    events = {},
	    grid = window.grid = {};

	( function()
	{

	})
	  .apply( util );


	( function()
	{

	})
	  .apply( events );


	( function()
	{
		var createGrid = function( args )
		{
			
		};

		this.Grid = function( args )
		{
			return createGrid( args );
		};

		( function()
		{
			
		})
		  .apply( this.Grid.prototype );
	})
	  .apply( grid );

})
( window, document );